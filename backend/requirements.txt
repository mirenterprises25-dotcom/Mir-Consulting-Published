# MIR Consulting backend — direct dependencies only.
# Version ranges use lower-bound pinning + next-major upper bound so pip can
# auto-resolve security patches without risking breaking-change updates.

# --- Web framework + ASGI server ---------------------------------------------
fastapi>=0.110,<1.0
uvicorn[standard]>=0.25,<1.0
python-multipart>=0.0.20,<1.0      # multipart/form-data uploads (admin media)
starlette>=0.37,<1.0               # explicit pin — CORSMiddleware import path

# --- Database ----------------------------------------------------------------
motor>=3.3,<4.0                    # async MongoDB driver (pulls pymongo)

# --- Data validation ---------------------------------------------------------
pydantic[email]>=2.5,<3.0          # `[email]` extra adds email-validator

# --- Config & utils ----------------------------------------------------------
python-dotenv>=1.0,<2.0
python-slugify>=8.0,<9.0
slowapi>=0.1.9,<1.0                # rate limiting

# --- Auth --------------------------------------------------------------------
bcrypt>=4.0,<5.0                   # password hashing (admin_settings)

# --- HTTP client (GitHub Contents API proxy) ---------------------------------
httpx>=0.27,<1.0

# --- Payments ----------------------------------------------------------------
stripe>=14.0,<15.0                 # Stripe Checkout SDK (portable; no vendor wrapper)

# --- AI / LLM translation ----------------------------------------------------
litellm>=1.50,<2.0                 # vendor-agnostic LLM client (OpenAI/Gemini/Anthropic)

# --- PDF generation ----------------------------------------------------------
reportlab>=4.0,<5.0                # invoice PDF rendering
pillow>=10.0,<13.0                 # required by reportlab for embedded images

# --- Tests (dev) -------------------------------------------------------------
pytest>=7.0,<10.0
